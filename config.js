tailwind.config = {
    theme: {
        fontFamily: {
            klee: "Klee One"
        },
        extend: {
            cursor: {
                'landcur': 'url("images/cursor.png"), default',
                'landpo': 'url("images/pointer.png"), pointer',
            }
        }
    },
};